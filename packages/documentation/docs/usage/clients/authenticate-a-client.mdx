import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import TOCInline from '@theme/TOCInline';

# Authenticate a Client

<TOCInline
  maxHeadingLevel={4}
  toc={toc}
/>

## Overview

If supported, providers can sign some typed authentication data.

:::info

When verifying the signature, it **SHOULD** be signed using the [ARC-0060](https://arc.algorand.foundation/ARCs/arc-0060) specification. The utility method [`verifyTypedData`](../../api-reference/utilities#verifytypeddataoptions) can be used to verify the signature.

:::

## Authenticate a client

<Tabs
  defaultValue="javascript"
  values={[
    { label: 'Javascript', value: 'javascript' },
    { label: 'TypeScript', value: 'typescript' },
  ]}>
  <TabItem value="javascript">

```js
import { verifyTypedData } from '@agoralabs-sh/avm-web-provider';
import { decodeAddress } from 'algosdk';

const authenticationData = await window.crypto.subtle.digest(
  'SHA-256',
  new TextEncoder().encode(window.location.hostname),
);
const data = {
  hello: 'humie',
};

// initialized client
client.onAuthenticate(({ error, result }) => {
  if (error) {
    console.error('error:', error);

    return;
  }

  console.log(result);
  /*
  {
    providerId: '02657eaf-be17-4efc-b0a4-19d654b2448e',
    signature: 'gqNzaWfEQ...',
    signer: 'P3AIQVDJ2CTH54KSJE63YWB7IZGS4W4JGC53I6GK72BGZ5BXO2B2PS4M4U',
  }
  */

  if (
    verifyTypedData({
      authenticationData,
      data,
      publicKey: decodeAddress('P3AIQVDJ2CTH54KSJE63YWB7IZGS4W4JGC53I6GK72BGZ5BXO2B2PS4M4U').publicKey,
      scope: 1, // AUTH
      signature: new TextEncoder().encode(window.bota(result.signature)),
    })
  ) {
    console.log('authentication successful');
  }
});

// send an authentication request
client.authenticate({
  authenticationData: window.bota(new TextDecoder.decode(authenticationData)),
  data: window.btoa(JSON.stringify(data)),
  providerId: '02657eaf-be17-4efc-b0a4-19d654b2448e',
});
```

  </TabItem>
  <TabItem value="typescript">

```typescript
import { verifyTypedData } from '@agoralabs-sh/avm-web-provider';
import { decodeAddress } from 'algosdk';

const authenticationData = await window.crypto.subtle.digest(
  'SHA-256',
  new TextEncoder().encode(window.location.hostname),
);
const data = {
  hello: 'humie',
};

// initialized client
client.onAuthenticate(({ error, result }) => {
  if (error) {
    console.error('error:', error);

    return;
  }

  console.log(result);
  /*
  {
    providerId: '02657eaf-be17-4efc-b0a4-19d654b2448e',
    signature: 'gqNzaWfEQ...',
    signer: 'P3AIQVDJ2CTH54KSJE63YWB7IZGS4W4JGC53I6GK72BGZ5BXO2B2PS4M4U',
  }
  */

  if (
    verifyTypedData({
      authenticationData,
      data,
      publicKey: decodeAddress('P3AIQVDJ2CTH54KSJE63YWB7IZGS4W4JGC53I6GK72BGZ5BXO2B2PS4M4U').publicKey,
      scope: 1, // AUTH
      signature: new TextEncoder().encode(window.bota(result.signature)),
    })
  ) {
    console.log('authentication successful');
  }
});

// send an authentication request
client.authenticate({
  authenticationData: window.bota(new TextDecoder.decode(authenticationData)),
  data: window.btoa(JSON.stringify(data)),
  providerId: '02657eaf-be17-4efc-b0a4-19d654b2448e',
});
```

  </TabItem>
</Tabs>

:::caution

If this method is not supported, then a [`VIP030027MethodNotSupportedError`](/api-reference/errors#vip030027methodnotsupportederror) **SHOULD** be sent in the `error` parameter.

:::

## Authenticate a client with a specific account

<Tabs
  defaultValue="javascript"
  values={[
    { label: 'Javascript', value: 'javascript' },
    { label: 'TypeScript', value: 'typescript' },
  ]}>
  <TabItem value="javascript">

```js
import { verifyTypedData } from '@agoralabs-sh/avm-web-provider';
import { decodeAddress } from 'algosdk';

const authenticationData = await window.crypto.subtle.digest(
  'SHA-256',
  new TextEncoder().encode(window.location.hostname),
);
const data = {
  hello: 'humie',
};
const signer = 'P3AIQVDJ2CTH54KSJE63YWB7IZGS4W4JGC53I6GK72BGZ5BXO2B2PS4M4U';

// initialized client
client.onAuthenticate(({ error, result }) => {
  if (error) {
    console.error('error:', error);

    return;
  }

  console.log(result);
  /*
  {
    providerId: '02657eaf-be17-4efc-b0a4-19d654b2448e',
    signature: 'gqNzaWfEQ...',
    signer: 'P3AIQVDJ2CTH54KSJE63YWB7IZGS4W4JGC53I6GK72BGZ5BXO2B2PS4M4U',
  }
  */

  if (
    verifyTypedData({
      authenticationData,
      data,
      publicKey: decodeAddress(signer).publicKey,
      scope: 1, // AUTH
      signature: new TextEncoder().encode(window.bota(result.signature)),
    })
  ) {
    console.log('authentication successful');
  }
});

// send an authentication request
client.authenticate({
  authenticationData: window.bota(new TextDecoder.decode(authenticationData)),
  data: window.btoa(JSON.stringify(data)),
  providerId: '02657eaf-be17-4efc-b0a4-19d654b2448e',
  signer,
});
```

  </TabItem>
  <TabItem value="typescript">

```typescript
import { verifyTypedData } from '@agoralabs-sh/avm-web-provider';
import { decodeAddress } from 'algosdk';

const authenticationData = await window.crypto.subtle.digest(
  'SHA-256',
  new TextEncoder().encode(window.location.hostname),
);
const data = {
  hello: 'humie',
};
const signer = 'P3AIQVDJ2CTH54KSJE63YWB7IZGS4W4JGC53I6GK72BGZ5BXO2B2PS4M4U';

// initialized client
client.onAuthenticate(({ error, result }) => {
  if (error) {
    console.error('error:', error);

    return;
  }

  console.log(result);
  /*
  {
    providerId: '02657eaf-be17-4efc-b0a4-19d654b2448e',
    signature: 'gqNzaWfEQ...',
    signer: 'P3AIQVDJ2CTH54KSJE63YWB7IZGS4W4JGC53I6GK72BGZ5BXO2B2PS4M4U',
  }
  */

  if (
    verifyTypedData({
      authenticationData,
      data,
      publicKey: decodeAddress(signer).publicKey,
      scope: 1, // AUTH
      signature: new TextEncoder().encode(window.bota(result.signature)),
    })
    ) {
    console.log('authentication successful');
  }
});

// send an authentication request
client.authenticate({
  authenticationData: window.bota(new TextDecoder.decode(authenticationData)),
  data: window.btoa(JSON.stringify(data)),
  providerId: '02657eaf-be17-4efc-b0a4-19d654b2448e',
  signer,
});
```

  </TabItem>
</Tabs>

:::caution

If the signer is not known, or not authorized, then a [`ARC0060InvalidSignerError`](../../api-reference/errors#arc0060invalidsignererror) will be sent in the `error` parameter.

:::
