import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import TOCInline from '@theme/TOCInline';

# Responding to Enable Requests

<TOCInline
  maxHeadingLevel={4}
  toc={toc}
/>

## Overview

The main purpose of an enable request is for clients to get a list of authorized accounts.

:::note

While not in the scope of this provider, it is **RECOMMENDED** that providers implement session logic.

:::

## Responding to an enable request

Once the provider object has been initialized, simply listen to events and respond:

<Tabs
  defaultValue="javascript"
  values={[
    { label: 'Javascript', value: 'javascript' },
    { label: 'TypeScript', value: 'typescript' },
  ]}>
  <TabItem value="javascript">

  ```js
  provider.onEnable(({ challenge, params }) => {
    return {
      result: {
        accounts: [
          {
            address: 'P3AIQVDJ2CTH54KSJE63YWB7IZGS4W4JGC53I6GK72BGZ5BXO2B2PS4M4U',
            name: 'Wallet-1',
          },
          {
            address: '6GT6EXFDAHZDZYUOPT725ZRWYBZDCEGYT7SYYXGJKRFUAG5B7JMI7DQRNQ',
            name: 'Wallet-2',
          },
        ],
        genesisHash: 'SGO1GKSzyE7IEPItTxCByw9x8FmnrCDexi9/cOUJOiI=',
        genesisID: 'testnet-v1.0',
        sessionID: '02657eaf-be17-4efc-b0a4-19d654b2448e', // optional
      },
      signature: 'gqNzaWfEQ...', // this must be the result of the signing of the challenge using the vcic's private key
      vcic: vcic.config.credential.toString(),
    };
  });
  ```

  </TabItem>
  <TabItem value="typescript">

  ```typescript
  provider.onEnable(({ challenge, params }) => {
    return {
      result: {
        accounts: [
          {
            address: 'P3AIQVDJ2CTH54KSJE63YWB7IZGS4W4JGC53I6GK72BGZ5BXO2B2PS4M4U',
            name: 'Wallet-1',
          },
          {
            address: '6GT6EXFDAHZDZYUOPT725ZRWYBZDCEGYT7SYYXGJKRFUAG5B7JMI7DQRNQ',
            name: 'Wallet-2',
          },
        ],
        genesisHash: 'SGO1GKSzyE7IEPItTxCByw9x8FmnrCDexi9/cOUJOiI=',
        genesisID: 'testnet-v1.0',
        sessionID: '02657eaf-be17-4efc-b0a4-19d654b2448e', // optional
      },
      signature: 'gqNzaWfEQ...', // this must be the result of the signing of the challenge using the vcic's private key
      vcic: vcic.config.credential.toString(),
    };
  });
  ```

  </TabItem>
</Tabs>

:::caution

If this method is not supported, then a [`VIP030027MethodNotSupportedError`](../../api-reference/errors.mdx#vip030027methodnotsupportederror) **SHOULD** be thrown.

:::

## Responding to an enable request for a specific network

The `enable` request allows the client to specify the network. This is specified by the `params.genesisHash` parameter.

<Tabs
  defaultValue="javascript"
  values={[
    { label: 'Javascript', value: 'javascript' },
    { label: 'TypeScript', value: 'typescript' },
  ]}>
  <TabItem value="javascript">

    ```js
    const { VIP030027NetworkNotSupportedError } = require('@agoralabs-sh/avm-web-provider');

    const genesisHash = 'SGO1GKSzyE7IEPItTxCByw9x8FmnrCDexi9/cOUJOiI=';

    provider.onEnable(({ challenge, params }) => {
      // if the genesis hash has been defined, it is recommended that you throw and error
      if (param.genesisHash && param.genesisHash !== genesisHash) {
        throw new VIP030027NetworkNotSupportedError({
          genesisHashes: [param.genesisHash],
          vcic: provider.config.credential.toString(),
        });
      }

      return {
        result: {
          accounts: [
            {
              address: 'P3AIQVDJ2CTH54KSJE63YWB7IZGS4W4JGC53I6GK72BGZ5BXO2B2PS4M4U',
              name: 'Wallet-1',
            },
            {
              address: '6GT6EXFDAHZDZYUOPT725ZRWYBZDCEGYT7SYYXGJKRFUAG5B7JMI7DQRNQ',
              name: 'Wallet-2',
            },
          ],
          genesisHash,
          genesisID: 'testnet-v1.0',
          sessionID: '02657eaf-be17-4efc-b0a4-19d654b2448e', // optional
        },
        signature: 'gqNzaWfEQ...', // this must be the result of the signing of the challenge using the vcic's private key
        vcic: vcic.config.credential.toString(),
      };
    });
    ```

  </TabItem>
  <TabItem value="typescript">

    ```typescript
    import { VIP030027NetworkNotSupportedError } from '@agoralabs-sh/avm-web-provider';

    const genesisHash = 'SGO1GKSzyE7IEPItTxCByw9x8FmnrCDexi9/cOUJOiI=';

    provider.onEnable(({ challenge, params }) => {
      // if the genesis hash has been defined, it is recommended that you throw and error
      if (param.genesisHash && param.genesisHash !== genesisHash) {
        throw new VIP030027NetworkNotSupportedError({
          genesisHashes: [param.genesisHash],
          vcic: provider.config.credential.toString(),
        });
      }

      return {
        result: {
          accounts: [
            {
              address: 'P3AIQVDJ2CTH54KSJE63YWB7IZGS4W4JGC53I6GK72BGZ5BXO2B2PS4M4U',
              name: 'Wallet-1',
            },
            {
              address: '6GT6EXFDAHZDZYUOPT725ZRWYBZDCEGYT7SYYXGJKRFUAG5B7JMI7DQRNQ',
              name: 'Wallet-2',
            },
          ],
          genesisHash,
          genesisID: 'testnet-v1.0',
          sessionID: '02657eaf-be17-4efc-b0a4-19d654b2448e', // optional
        },
        signature: 'gqNzaWfEQ...', // this must be the result of the signing of the challenge using the vcic's private key
        vcic: vcic.config.credential.toString(),
      };
    });
    ```

  </TabItem>
</Tabs>

:::caution

If a network is specified, and the provider does not support it, then a [`VIP030027NetworkNotSupportedError`](../../api-reference/errors.mdx#vip030027networknotsupportederror) **SHOULD** be thrown.

:::
